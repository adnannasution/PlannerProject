@model Plan.Models.DisiplinMaster

@{
    ViewData["Title"] = "Disiplin Master";

            ViewData["Title1"] = "Disiplin Master";
    ViewData["Title2"] = "Delete";

}



<div>
    <h4>Are you sure you want to delete this?</h4>
    <div>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Disiplin)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Disiplin)
            </dd>
        </dl>
    </div>
</div>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>
